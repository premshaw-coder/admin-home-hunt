#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# npx eslint . --cache --ext .ts,.html --fix

# Basic check: prevent empty commit messages
if test -z "$(cat "$1")"; then
  echo "❌ Commit message cannot be empty."
  exit 1
fi

